# TODO - 核心模块开发任务

## 配置扩展

### 高优先级

- [ ] **配置热重载**
  - 配置文件修改后自动重载
  - 不中断服务
  - 配置验证

- [ ] **配置加密**
  - 敏感配置（数据库密码/Secret Key）加密存储
  - 支持环境变量覆盖

### 中优先级

- [ ] **配置验证**
  - 启动时验证配置完整性
  - 缺少必填项时报错
  - 配置文档生成

## 安全扩展

- [ ] **Token 黑名单**: Redis 存储已撤销的 Token
- [ ] **刷新 Token**: 支持 Token 刷新机制
- [ ] **OAuth2 支持**: 第三方登录集成

## 日志扩展

- [ ] **日志聚合**: 结构化日志输出到统一日志系统
- [ ] **链路追踪**: 请求 ID 贯穿整个调用链
- [ ] **敏感信息过滤**: 日志中自动过滤密码/Token

## 验收检查清单

- [ ] `get_config()` 正确读取 settings.yaml
- [ ] `create_access_token()` 生成有效 JWT
- [ ] `@require_permission()` 正确检查权限
- [ ] `get_logger()` 返回正确格式的日志器