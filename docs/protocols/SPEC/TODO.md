# TODO - 协议处理开发任务

## 协议扩展

### 高优先级

- [ ] **WebSocket 协议**
  - 新增 `WebSocketHandler`
  - 继承 `ProtocolHandler`
  - 支持实时双向通信
  - 权限：已认证用户

- [ ] **协议路由统一**
  - 所有协议调用统一的命令执行接口
  - 命令结果标准化输出
  - 协议间状态同步

### 中优先级

- [ ] **协议中间件**
  - 请求日志中间件
  - 认证中间件
  - 权限检查中间件

- [ ] **协议超时处理**
  - 统一超时配置
  - 超时返回标准错误

## 验收检查清单

- [ ] `HTTPHandler` 处理 REST 请求正常
- [ ] `SSHHandler` 处理终端命令正常
- [ ] `WebSocketHandler` 支持实时通信
- [ ] 新协议可继承 `ProtocolHandler` 添加