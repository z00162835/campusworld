# TODO - SSH 开发任务

## 验收追踪

- 统一验收文档：`docs/ssh/SPEC/ACCEPTANCE.md`
- 目标：所有入口相关任务可映射到“成功/恢复/拒绝/降级”四类场景。

## 功能扩展

### 高优先级

- [ ] **会话持久化**
  - 用户离线时保存当前位置
  - 下次登录时恢复到上次离开的位置
  - 存储到数据库或 Redis
  - 验收映射：`ACCEPTANCE 场景 B`

- [ ] **公钥认证支持**
  - 支持 SSH 公钥认证
  - 用户上传公钥后可用密钥登录
  - 权限：已认证用户

- [ ] **命令执行超时机制**
  - 长时间运行的命令设置超时
  - 超时后强制终止并返回错误
  - 超时时间可配置
  - 验收映射：`ACCEPTANCE 稳定性观察点`

### 中优先级

- [ ] **会话监控增强**
  - 显示每个会话的空闲时间
  - 空闲超时时发送提醒
  - 空闲超时过长发自动断开

- [ ] **终端渲染优化**
  - 支持 ANSI 颜色输出
  - 支持进度条显示
  - 表格化输出（如 who 命令）

- [ ] **命令补全增强**
  - 空间名称补全
  - 方向名称补全（north/south/east/west）
  - 历史命令补全

### 低优先级

- [ ] **多语言支持**: 终端输出国际化
- [ ] **快捷键支持**: Ctrl+C 中断命令 / Ctrl+L 清屏
- [ ] **脚本录制**: 录制和回放命令序列

## 安全增强

| 功能 | 说明 | 优先级 |
|------|------|--------|
| IP 白名单 | 特定 IP 绕过速率限制 | 中 |
| 会话加密 | 强制使用加密算法 | 低 |
| 审计日志 | 记录所有命令执行 | 中 |
| 会话隔离 | 防止会话间数据泄露 | 高 |

## 验收检查清单

- [ ] `ssh localhost -p 2222` 能建立连接
- [ ] 正确密码登录成功，进入 `SingularityRoom` 或恢复位置（按策略）
- [ ] 错误密码 5 次后，IP 锁定 300 秒
- [ ] `look` 命令返回当前位置信息
- [ ] `quit` 命令正确断开连接
- [ ] 50 个并发会话正常运行
- [ ] 无权限/不可用世界请求失败后，用户保持在 `SingularityRoom`